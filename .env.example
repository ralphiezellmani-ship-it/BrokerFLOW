# ============================================================
# BrokerFlow — Miljövariabler
# ============================================================
# Kopiera denna fil till .env.local och fyll i värdena.
# COMMITTA ALDRIG .env.local till git!
# ============================================================

# ----------------------------------------------------------
# Supabase
# ----------------------------------------------------------
# URL till din Supabase-instans (lokal: http://localhost:54321)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321

# Anon-nyckel (publik, säker att exponera i klienten)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role-nyckel (HEMLIG — används bara server-side)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----------------------------------------------------------
# AI (abstraherat LLM-interface)
# ----------------------------------------------------------
# Välj provider: 'anthropic' eller 'openai'
LLM_PROVIDER=anthropic

# Anthropic API-nyckel (krävs om LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API-nyckel (krävs om LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-...

# ----------------------------------------------------------
# E-post
# ----------------------------------------------------------
# Resend API-nyckel (utgående e-post)
RESEND_API_KEY=re_...

# Mailgun webhook signing key (inkommande e-post)
MAILGUN_WEBHOOK_SIGNING_KEY=...

# Domän för inkommande e-post
INBOUND_EMAIL_DOMAIN=in.brokerflow.se

# ----------------------------------------------------------
# Betalning (Stripe)
# ----------------------------------------------------------
# Stripe hemlig nyckel (server-side)
STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook-signerings-hemlighet
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe publik nyckel (säker att exponera i klienten)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# ----------------------------------------------------------
# App
# ----------------------------------------------------------
# Appens bas-URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Hemlig nyckel för att skydda cron-endpoints
CRON_SECRET=your-cron-secret
